{% extends 'AdminBundle:Default:admin.html.twig' %}

{% block content %}
<div class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <div class="card strpied-tabled-with-hover">
                    <div class="card-header ">
                        <h4 class="card-title">Correos</h4>
                        <p class="card-category">Subtitulo</p>
                        <a href="{{ path ('admin_test_newsletter') }}"><button type="button" class="btn btn-warning btn-fill pull-right">Enviar prueba</button></a>
                        <a href="{{ path ('admin_newsletter') }}"><button type="button" class="btn btn-info btn-fill pull-left">Ver email de prueba Newsletter</button></a>
                    </div>
                    <div class="card-body table-full-width table-responsive">
                        {% include 'AdminBundle:Mailer:newsletter.html.twig' with {'blogs': blogs} %}
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block script %}
<script type="text/javascript">
    $('.mails').addClass('nav-item active');
    $('.navbar-brand').text('{{"admin.mails"|trans}}');
</script>
{% endblock %}